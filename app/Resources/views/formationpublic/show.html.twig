{% extends 'base.html.twig' %}

{% block body %}
    <h1>Formationpublic</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formationPublic.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('formationpublic_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('formationpublic_edit', { 'id': formationPublic.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
