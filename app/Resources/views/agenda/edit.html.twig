{% extends 'AdminBundle::adminLayout.html.twig' %}
{% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}
{% block body %}

<div class="container-fluid">

    <h1>Edition agenda</h1>
{#
    {{ include ('AdminBundle:Default:listAgenda.html.twig') }}
#}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="row">
            <div class="col-sm-4 col-sm-offset-4">
                <input class="btn btn-info  btn-lg btn-block" type="submit" value="Edit" />
            </div>
        </div>    {{ form_end(edit_form) }}



        {{ form_start(delete_form) }}
            <input class="btn btn-danger pull-right" type="submit" value="Delete">
        {{ form_end(delete_form) }}

        <a class="btn btn-primary" href="{{ path('agenda_index') }}">Back to the list</a>

    </div>
{% endblock %}
