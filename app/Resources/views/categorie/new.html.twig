{% extends 'AdminBundle::adminLayout.html.twig' %}
{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block body %}

    <h1 xmlns="http://www.w3.org/1999/html">Catégorie création</h1>

    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <div class="row">
            <div class="col-sm-4 col-sm-offset-4">
                <input class="btn btn-success  btn-lg btn-block" type="submit" value="Créer" />
            </div>
        </div>
    {{ form_end(form) }}

        <a class="btn btn-primary" href="{{ path('categorie_index') }}">Retour à la liste</a>
    </div>
{% endblock %}

