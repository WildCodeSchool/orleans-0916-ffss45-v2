{% extends 'AdminBundle::adminLayout.html.twig' %}
{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block body %}
    <h1>Agendas de {{ formation.nomCourt }}</h1>
    <div class="container-fluid">
        {{ include ('AdminBundle:Default:listAgenda.html.twig') }}
        <div class="">
            <h2>Ajouter un nouvel agenda</h2>
        {{ form_start(form) }}
            {{ form_errors(form.dateDeDebut) }}
            {{ form_label(form.dateDeDebut) }}
            {{ form_widget(form.dateDeDebut) }}

            {{ form_errors(form.dateDeFin) }}
            {{ form_label(form.dateDeFin) }}
            {{ form_widget(form.dateDeFin) }}

          {#  {{ form_widget(form) }}#}
            <div class="row">
                <div class="col-sm-4 col-sm-offset-4">
                    <input class="btn btn-success  btn-lg btn-block" type="submit" value="Create" />
                </div>
            </div>
        {{ form_end(form) }}
        <a class="btn btn-primary btn-sm   " href="{{ path('formation_index') }}">Retour aux formations</a>
        </div>
    </div>

{% endblock %}